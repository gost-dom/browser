// Packaage clock represents a virtual clock, as well future tasks.
//
// The use case of clock is abstract time for tests, allowing tests to simulate
// time passes. Future operations can be registered, and they will execute as
// simulated time advances.
package clock
